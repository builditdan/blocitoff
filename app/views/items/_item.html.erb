<tr class="js-a-item-<%=item.id=%>">
  <td>
    <%= item.name %></br>
  </td>

  <td>

    <%= distance_of_time_in_words(Time.now, item.created_at)   %>
  </td>

  <td>
    <%= link_to "", user_item_path(item.user_id, item.id), method: :delete, remote: true, class: 'glyphicon glyphicon-ok' %>
    <%# link_to "", show_items_path, method: :delete, class: 'glyphicon glyphicon-ok' %>
  </td>
</tr>
